// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // add your project configuration here ie site, integrations, build options, etc.
});
